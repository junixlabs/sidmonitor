[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    e2e: marks tests as end-to-end tests
    slow: marks tests as slow running (may take > 5s)
filterwarnings =
    ignore::DeprecationWarning
